import { FC, HTMLProps, PropsWithChildren } from 'react';

// TODO: Make actual button component
export const Button: FC<HTMLProps<HTMLButtonElement> & PropsWithChildren> = ({
    ...properties
}) => {
    return <div></div>;
};
