export const verifyDNS = async (domain: string, user: string) => {
    // Traverse DNS until we find a TXT record with the correct value otherwise return false

    // TODO: Implement DNS Verification

    return false;
};
